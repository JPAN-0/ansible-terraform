default:
    vsphere_host: "{{vcenter_host}}"
    vsphere_user: "{{vault_vcenter_user}}"
    vsphere_password: "{{vault_vcenter_password}}"
    ignore_ssl: True
    specs_size: 5000
    fetch_custom_attributes: True
    fetch_tags: True
    fetch_alarms: True
    collect_only:
        vms: True
        vmguests: True
        datastores: True
        hosts: True
        snapshots: True
